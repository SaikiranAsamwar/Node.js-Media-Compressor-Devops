apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: media-app
type: Opaque
data:
  # IMPORTANT: Replace these base64 encoded values with your own secrets
  # Generate with: echo -n 'your-secret' | base64
  jwt-secret: eW91ci1qd3Qtc2VjcmV0LWNoYW5nZS10aGlzLXByb2R1Y3Rpb24=      # base64("your-jwt-secret-change-this-production")
  session-secret: eW91ci1zZXNzaW9uLXNlY3JldC1jaGFuZ2UtdGhpcy1wcm9kdWN0aW9u  # base64("your-session-secret-change-this-production")

---
# INSTRUCTIONS FOR PRODUCTION DEPLOYMENT:
# 
# 1. Generate strong secrets:
#    JWT_SECRET=$(openssl rand -base64 32)
#    SESSION_SECRET=$(openssl rand -base64 32)
#
# 2. Base64 encode them:
#    echo -n "$JWT_SECRET" | base64
#    echo -n "$SESSION_SECRET" | base64
#
# 3. Replace the values above with your encoded secrets
#
# 4. Apply the secret:
#    kubectl apply -f k8s/backend/backend-secrets.yaml